interface:
  display_name: "Blend Micro Motion"
  short_description: "Direct small illustrated and interactive sequences"
  default_prompt: "Use $blend-micro-motion to clarify the drawing language, action, trigger, timing, and replay behavior, then create a clean local sequence."
