interface:
  display_name: "Blend"
  short_description: "Interview first, then build a coherent frontend"
  default_prompt: "Use $blend to ask me the important design questions before building this frontend, then carry my choices through the relevant specialists."
