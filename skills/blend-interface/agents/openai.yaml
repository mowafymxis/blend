interface:
  display_name: "Blend Interface"
  short_description: "Build coherent app workflows and UI states"
  default_prompt: "Use $blend-interface to clarify unresolved workflow and design choices for this app, then build its interface while preserving existing scope and behavior."
